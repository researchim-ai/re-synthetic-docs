"""Prompts and output schemas for OCR tasks (text-only and text+bboxes).

This module is Apache-2.0 compatible and intended to be imported from notebooks.
"""

INSTR_OCR_STRICT: str = (
    "Внимательно распознай весь печатный текст на изображении. "
    "Выведи ТОЛЬКО распознанный текст без каких-либо комментариев, разметки, префиксов и кавычек. "
    "Сохраняй порядок строк и знаки препинания. Если символ неразборчив, не выдумывай его."
)

INSTR_OCR_BBOX_JSON: str = (
    "Распознай текст и найди области подписи (signature) и печати (stamp) на изображении. "
    "Верни строго валидный JSON в следующем формате без пояснений и без разметки Markdown:\n"
    "{\n"
    "  \"text\": string,\n"
    "  \"bboxes\": [\n"
    "    { \"type\": \"signature\" | \"stamp\", \"coords\": [x1, y1, x2, y2] }\n"
    "  ]\n"
    "}\n"
    "Где coords — плавающие значения (или целые) координат углов прямоугольника в пикселях изображения: (лево, верх, право, низ)."
)

JSON_OUTPUT_HINT: str = (
    "Верни только JSON. Не добавляй префиксы, комментарии, кодовые блоки или текст до/после."
)


